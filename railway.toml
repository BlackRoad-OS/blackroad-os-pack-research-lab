[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run start:lab"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
PORT = "8080"
SERVICE_NAME = "blackroad-os-pack-research-lab"
